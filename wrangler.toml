name = "streamcast-worker"
main            = "worker.js"
compatibility_date = "2025-01-01"

# Free tier: 100,000 requests/day, no KV needed (using Cache API)
# No special bindings required — Cache API is available on all plans

[observability]
enabled = true   # free log streaming in the dashboard
